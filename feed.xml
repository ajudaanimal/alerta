---
layout: null
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator>
  <link href="{{ site.url }}{{ site.baseurl }}/feed.xml" rel="self" type="application/atom+xml"/>
  <link href="{{ site.url }}{{ site.baseurl }}/" rel="alternate" type="text/html"/>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ site.url }}{{ site.baseurl }}/feed.xml</id>
  <title type="html">{{ site.title | xml_escape }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>

  {% for post in site.ocorrencias reversed limit:20 %}
  <entry>
    <title>[{{ post.id }}] {{ post.especie }} - {{ post.localizacao }}</title>
    <link href="{{ site.url }}{{ site.baseurl }}{{ post.url }}" rel="alternate" type="text/html"/>
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.date | date_to_xmlschema }}</updated>
    <id>{{ site.url }}{{ site.baseurl }}{{ post.url }}</id>
    <content type="html"><![CDATA[
      <p><strong>ID:</strong> {{ post.id }}</p>
      <p><strong>Espécie:</strong> {{ post.especie }}</p>
      <p><strong>Estado Físico:</strong> {{ post.estado_fisico }}</p>
      <p><strong>Situação:</strong> {{ post.situacao }}</p>
      <p><strong>Localização:</strong> {{ post.localizacao }}</p>
      {% if post.imagem != "" %}
        <p><img src="{{ post.imagem }}" alt="Imagem" /></p>
      {% endif %}
      <p><strong>Observações:</strong> {{ post.observacoes }}</p>
      <p><a href="{{ site.url }}{{ site.baseurl }}{{ post.url }}">Ver ocorrência completa no site</a></p>
    ]]></content>
  </entry>
  {% endfor %}
</feed>
